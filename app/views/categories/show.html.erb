<h1><%= @category.name %></h1>
<div class="navbar mini">
	<ul>
		<li><%= link_to "Recommend", new_category_recommend_path(@category) %></li>
	</ul>
</div>
<br>
<div>
	<% @brands.each do |brand| %>
	<%= render partial: "brands/brands", locals: {brand: brand} %>
	<% end %>
</div>
