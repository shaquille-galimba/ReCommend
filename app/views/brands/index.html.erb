<h1>Browse for brands</h1>
<%= render partial: "shared/search", locals: {path: brands_path} %>
<br>

<% @brands.each do |brand| %>
	<%= render partial: "shared/name_list", locals: {zone: brand, path: new_brand_recommend_path(brand)} %>
<% end %>
